---
description: 开源参考库上下文 — 写代码时先查 docs/refs-index，再读 refs/ 源码
alwaysApply: true
---

# 开源参考库（只读）

`refs/` 存放依赖库源码，仅供 Cursor 参照。**不要修改**其中任何文件，也不要提交到版本库。

## 检索流程

1. **[docs/refs-index/README.md](../../docs/refs-index/README.md)** — 总导航、**检索关键词表**、POC 速查表
2. 按场景读子索引：`epubx.md` / `fwfh.md` / `drift.md`
3. 多步骤任务 → **[scenario-map.md](../../docs/refs-index/scenario-map.md)**
4. 打开 `refs/` 中 1–3 个关键文件核对 API（禁止编造类名/方法）

> 关键词与速查路径**只在 `docs/refs-index/README.md` 维护**，本 rule 不重复。

## 使用原则

1. 导入管线 → `epubx.md` → `EpubReader`、`EpubChapterRef`
2. HTML 高亮与查词 → `fwfh.md` → `WidgetFactory` 扩展
3. Drift Schema → `drift.md` → `examples/app` 的 Table/DAO 模式
4. refs 与 `docs/tech-stack.md` / `docs/data-model.md` 冲突时，**以项目文档为准**
